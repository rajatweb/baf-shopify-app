export interface ThemeStatusResponse {
  success: boolean;
  shopUrl: string;
  themeId: number;
  themeName: string;
  isThemeExtensionDisabled: boolean;
}

export interface AppIdResponse {
  success: boolean;
  id: string;
}
